// auto generated by dev/system

export function make_input(
    cb?: (o: {
        name: string;
        version: string;
        with_customize_dir: boolean;
        custom_build_steps: { library_name: string; function_name: string; function_input: { [key: string]: any } }[];
    }) => void
): {
    name: string;
    version: string;
    with_customize_dir: boolean;
    custom_build_steps: { library_name: string; function_name: string; function_input: { [key: string]: any } }[];
} {
    const item: {
        name: string;
        version: string;
        with_customize_dir: boolean;
        custom_build_steps: { library_name: string; function_name: string; function_input: { [key: string]: any } }[];
    } = { name: "", version: "", with_customize_dir: false, custom_build_steps: [] };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { text: string }) => void): { text: string } {
    const item: { text: string } = { text: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
