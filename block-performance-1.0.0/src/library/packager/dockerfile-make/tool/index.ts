// auto generated by dev/system

export function make_input(cb?: (o: { name: string; version: string; with_customize_dir: boolean }) => void): {
    name: string;
    version: string;
    with_customize_dir: boolean;
} {
    const item: { name: string; version: string; with_customize_dir: boolean } = { name: "", version: "", with_customize_dir: false };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { text: string }) => void): { text: string } {
    const item: { text: string } = { text: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
