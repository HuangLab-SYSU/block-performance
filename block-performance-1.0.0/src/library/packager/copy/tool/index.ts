// auto generated by dev/system

export function make_input(cb?: (o: { library_list: string[]; output_dir: string }) => void): { library_list: string[]; output_dir: string } {
    const item: { library_list: string[]; output_dir: string } = { library_list: [], output_dir: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
