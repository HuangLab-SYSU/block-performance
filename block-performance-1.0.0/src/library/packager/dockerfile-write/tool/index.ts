// auto generated by dev/system

export function make_input(
    cb?: (o: {
        name: string;
        version: string;
        with_customize_dir: boolean;
        custom_build_steps: { library_name: string; function_name: string; function_input: { [key: string]: any } }[];
        output_dir: string;
    }) => void
): {
    name: string;
    version: string;
    with_customize_dir: boolean;
    custom_build_steps: { library_name: string; function_name: string; function_input: { [key: string]: any } }[];
    output_dir: string;
} {
    const item: {
        name: string;
        version: string;
        with_customize_dir: boolean;
        custom_build_steps: { library_name: string; function_name: string; function_input: { [key: string]: any } }[];
        output_dir: string;
    } = { name: "", version: "", with_customize_dir: false, custom_build_steps: [], output_dir: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
