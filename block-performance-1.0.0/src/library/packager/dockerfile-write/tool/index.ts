// auto generated by dev/system

export function make_input(cb?: (o: { name: string; version: string; with_customize_dir: boolean; output_dir: string }) => void): {
    name: string;
    version: string;
    with_customize_dir: boolean;
    output_dir: string;
} {
    const item: { name: string; version: string; with_customize_dir: boolean; output_dir: string } = {
        name: "",
        version: "",
        with_customize_dir: false,
        output_dir: ""
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
