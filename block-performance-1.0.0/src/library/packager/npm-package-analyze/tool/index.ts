// auto generated by dev/system

export function make_input(cb?: (o: { input_dir: string }) => void): { input_dir: string } {
    const item: { input_dir: string } = { input_dir: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { npm_package_list: { name: string }[] }) => void): { npm_package_list: { name: string }[] } {
    const item: { npm_package_list: { name: string }[] } = { npm_package_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
