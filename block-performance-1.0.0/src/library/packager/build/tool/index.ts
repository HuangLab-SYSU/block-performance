// auto generated by dev/system

export function make_input(
    cb?: (o: {
        name: string;
        version: string;
        library_list: string[];
        npm_script: { dev: string; start: string; stop: string };
        customize_file_list: { name: string; json_data?: { [key: string]: any } }[];
        output_dir: string;
    }) => void
): {
    name: string;
    version: string;
    library_list: string[];
    npm_script: { dev: string; start: string; stop: string };
    customize_file_list: { name: string; json_data?: { [key: string]: any } }[];
    output_dir: string;
} {
    const item: {
        name: string;
        version: string;
        library_list: string[];
        npm_script: { dev: string; start: string; stop: string };
        customize_file_list: { name: string; json_data?: { [key: string]: any } }[];
        output_dir: string;
    } = { name: "", version: "", library_list: [], npm_script: { dev: "", start: "", stop: "" }, customize_file_list: [], output_dir: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { target_dir: string; target_tar_zip_file: string }) => void): { target_dir: string; target_tar_zip_file: string } {
    const item: { target_dir: string; target_tar_zip_file: string } = { target_dir: "", target_tar_zip_file: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
