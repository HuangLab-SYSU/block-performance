// auto generated by dev/system

export function make_input(cb?: (o: { filename: string; text: string; format: boolean }) => void): { filename: string; text: string; format: boolean } {
    const item: { filename: string; text: string; format: boolean } = { filename: "", text: "", format: false };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
