// auto generated by dev/system

export function make_input(cb?: (o: { filename: string }) => void): { filename: string } {
    const item: { filename: string } = { filename: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { text: string }) => void): { text: string } {
    const item: { text: string } = { text: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
