// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    file: string;
}

export interface OutputEmpty {
    npm_package_list: { name: string }[];
    node_module_list: { name: string }[];
    file_module_list: { js_name: string; js_relative_path: string; js_abs_path: string; ts_name: string; ts_relative_path: string; ts_abs_path: string }[];
}

export interface OutputOk {
    npm_package_list: { name: string }[];
    node_module_list: { name: string }[];
    file_module_list: { js_name: string; js_relative_path: string; js_abs_path: string; ts_name: string; ts_relative_path: string; ts_abs_path: string }[];
}

export interface OutputFail {}

export interface Callback<R> {
    empty: (output: OutputEmpty) => R;
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "file") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.file must be string");
        if (typeof v.file !== "string") {
            throw new Error("v.file is not string");
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_empty(v: any): OutputEmpty {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_empty(v_cloned);
    } else {
        return _copy_output_empty(v);
    }
}

export function _copy_output_empty(v: any): OutputEmpty {
    if (typeof v === "object" && v !== null) {
        const obj = {
            npm_package_list: copy_npm_package_list(v.npm_package_list),
            node_module_list: copy_node_module_list(v.node_module_list),
            file_module_list: copy_file_module_list(v.file_module_list)
        };
        return obj;
    } else {
        return { npm_package_list: [], node_module_list: [], file_module_list: [] };
    }

    function copy_npm_package_list(v: any): { name: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): { name: string } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    name: copy_name(v.name)
                };
                return obj;
            } else {
                return { name: "" };
            }

            function copy_name(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }

    function copy_node_module_list(v: any): { name: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): { name: string } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    name: copy_name(v.name)
                };
                return obj;
            } else {
                return { name: "" };
            }

            function copy_name(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }

    function copy_file_module_list(
        v: any
    ): { js_name: string; js_relative_path: string; js_abs_path: string; ts_name: string; ts_relative_path: string; ts_abs_path: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): {
            js_name: string;
            js_relative_path: string;
            js_abs_path: string;
            ts_name: string;
            ts_relative_path: string;
            ts_abs_path: string;
        } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    js_name: copy_js_name(v.js_name),
                    js_relative_path: copy_js_relative_path(v.js_relative_path),
                    js_abs_path: copy_js_abs_path(v.js_abs_path),
                    ts_name: copy_ts_name(v.ts_name),
                    ts_relative_path: copy_ts_relative_path(v.ts_relative_path),
                    ts_abs_path: copy_ts_abs_path(v.ts_abs_path)
                };
                return obj;
            } else {
                return { js_name: "", js_relative_path: "", js_abs_path: "", ts_name: "", ts_relative_path: "", ts_abs_path: "" };
            }

            function copy_js_name(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_js_relative_path(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_js_abs_path(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_ts_name(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_ts_relative_path(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_ts_abs_path(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {
            npm_package_list: copy_npm_package_list(v.npm_package_list),
            node_module_list: copy_node_module_list(v.node_module_list),
            file_module_list: copy_file_module_list(v.file_module_list)
        };
        return obj;
    } else {
        return { npm_package_list: [], node_module_list: [], file_module_list: [] };
    }

    function copy_npm_package_list(v: any): { name: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): { name: string } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    name: copy_name(v.name)
                };
                return obj;
            } else {
                return { name: "" };
            }

            function copy_name(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }

    function copy_node_module_list(v: any): { name: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): { name: string } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    name: copy_name(v.name)
                };
                return obj;
            } else {
                return { name: "" };
            }

            function copy_name(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }

    function copy_file_module_list(
        v: any
    ): { js_name: string; js_relative_path: string; js_abs_path: string; ts_name: string; ts_relative_path: string; ts_abs_path: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): {
            js_name: string;
            js_relative_path: string;
            js_abs_path: string;
            ts_name: string;
            ts_relative_path: string;
            ts_abs_path: string;
        } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    js_name: copy_js_name(v.js_name),
                    js_relative_path: copy_js_relative_path(v.js_relative_path),
                    js_abs_path: copy_js_abs_path(v.js_abs_path),
                    ts_name: copy_ts_name(v.ts_name),
                    ts_relative_path: copy_ts_relative_path(v.ts_relative_path),
                    ts_abs_path: copy_ts_abs_path(v.ts_abs_path)
                };
                return obj;
            } else {
                return { js_name: "", js_relative_path: "", js_abs_path: "", ts_name: "", ts_relative_path: "", ts_abs_path: "" };
            }

            function copy_js_name(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_js_relative_path(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_js_abs_path(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_ts_name(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_ts_relative_path(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_ts_abs_path(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
