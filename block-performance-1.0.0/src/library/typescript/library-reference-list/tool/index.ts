// auto generated by dev/system

export function make_input(cb?: (o: { library_name: string; recursive: boolean }) => void): { library_name: string; recursive: boolean } {
    const item: { library_name: string; recursive: boolean } = { library_name: "", recursive: false };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_empty(cb?: (o: { library_list: string[]; other_list: string[] }) => void): { library_list: string[]; other_list: string[] } {
    const item: { library_list: string[]; other_list: string[] } = { library_list: [], other_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { library_list: string[]; other_list: string[] }) => void): { library_list: string[]; other_list: string[] } {
    const item: { library_list: string[]; other_list: string[] } = { library_list: [], other_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
