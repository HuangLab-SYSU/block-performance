// auto generated by dev/system

export function make_input(cb?: (o: { file: string }) => void): { file: string } {
    const item: { file: string } = { file: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { result: any }) => void): { result: any } {
    const item: { result: any } = { result: null };
    if (cb) {
        cb(item);
    }
    return item;
}
