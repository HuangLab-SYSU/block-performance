// auto generated by dev/system

export function make_input(cb?: (o: { username: string; address: string }) => void): { username: string; address: string } {
    const item: { username: string; address: string } = { username: "", address: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { key_file_path: string }) => void): { key_file_path: string } {
    const item: { key_file_path: string } = { key_file_path: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
