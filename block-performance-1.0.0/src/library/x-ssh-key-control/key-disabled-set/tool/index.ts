// auto generated by dev/system

export function make_input(cb?: (o: { id: string; disabled: boolean }) => void): { id: string; disabled: boolean } {
    const item: { id: string; disabled: boolean } = { id: "", disabled: false };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
