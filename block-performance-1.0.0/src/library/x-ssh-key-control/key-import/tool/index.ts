// auto generated by dev/system

export function make_input(cb?: (o: { private_key_filename: string; username: string; address_list: string[]; tag_list: string[] }) => void): {
    private_key_filename: string;
    username: string;
    address_list: string[];
    tag_list: string[];
} {
    const item: { private_key_filename: string; username: string; address_list: string[]; tag_list: string[] } = {
        private_key_filename: "",
        username: "",
        address_list: [],
        tag_list: []
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { id: string }) => void): { id: string } {
    const item: { id: string } = { id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
