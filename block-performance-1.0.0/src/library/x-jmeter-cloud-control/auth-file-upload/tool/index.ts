// auto generated by dev/system

export function make_input(
    cb?: (o: {
        username: string;
        address: string;
        ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
        local_auth_file_path: string;
        remote_auth_file_path: string;
    }) => void
): {
    username: string;
    address: string;
    ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
    local_auth_file_path: string;
    remote_auth_file_path: string;
} {
    const item: {
        username: string;
        address: string;
        ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
        local_auth_file_path: string;
        remote_auth_file_path: string;
    } = { username: "", address: "", ssh_auth_key: {}, local_auth_file_path: "", remote_auth_file_path: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
