// auto generated by dev/system

export function make_input(cb?: (o: { master_server_id: string }) => void): { master_server_id: string } {
    const item: { master_server_id: string } = { master_server_id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
