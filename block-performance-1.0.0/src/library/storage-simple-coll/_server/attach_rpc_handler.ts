// auto generated by dev/system
import express from "express";
import { Logger } from "../../../myutils/logger.js";
import { handle_rpc_collection_item_add } from "../collection-item-add/rpc/handle.js";
import { handle_rpc_collection_item_del } from "../collection-item-del/rpc/handle.js";
import { handle_rpc_collection_item_get } from "../collection-item-get/rpc/handle.js";
import { handle_rpc_collection_item_ls } from "../collection-item-ls/rpc/handle.js";
import { handle_rpc_collection_item_set } from "../collection-item-set/rpc/handle.js";

export function attach_rpc_handler(plog: Logger, opt: { router: express.Router }) {
    const log = plog.sub("server.attach_rpc_handler");
    const { router } = opt;

    router.post("/library/storage-simple-coll/collection-item-add", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.storage-simple-coll.collection-item-add");
        handle_rpc_collection_item_add(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });

    router.post("/library/storage-simple-coll/collection-item-del", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.storage-simple-coll.collection-item-del");
        handle_rpc_collection_item_del(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });

    router.post("/library/storage-simple-coll/collection-item-get", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.storage-simple-coll.collection-item-get");
        handle_rpc_collection_item_get(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });

    router.post("/library/storage-simple-coll/collection-item-ls", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.storage-simple-coll.collection-item-ls");
        handle_rpc_collection_item_ls(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });

    router.post("/library/storage-simple-coll/collection-item-set", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.storage-simple-coll.collection-item-set");
        handle_rpc_collection_item_set(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });
}
