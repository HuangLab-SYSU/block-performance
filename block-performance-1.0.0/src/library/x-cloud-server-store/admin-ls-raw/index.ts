// auto generated by dev/system

import { Logger, set_logger_disabled } from "../../../myutils/logger.js";
import { prompts } from "../../../myutils/index.js";
import { read_json_file } from "../../../myutils/node/file/index.js";
import { Input, Output, check_input, OutputEmpty, OutputOk } from "./type.js";
import { admin_ls_raw as target_fun } from "./export.js";
import { rpc_admin_ls_raw as rpc_target_fun } from "./rpc/client.js";
import yargs from "yargs";

const log = new Logger("cli");
run();

async function run() {
    try {
        const { input, server } = await make_input(log, {
            ok: async (input: Input, server) => {
                return { input, server };
            },
            fail: async (err) => {
                throw err;
            }
        });

        if (server) {
            await rpc_target_fun(
                log,
                {
                    server,
                    input
                },
                {
                    empty: (output) => {
                        console.log(JSON.stringify(output, null, 4));
                        console.log("😄 empty");
                    },

                    ok: (output) => {
                        console.log(JSON.stringify(output, null, 4));
                        console.log("😄 ok");
                    },

                    fail: (err) => {
                        throw err;
                    }
                }
            );
        } else {
            await target_fun(log, input, {
                empty: (output: OutputEmpty) => {
                    console.log(JSON.stringify(output, null, 4));
                    console.log("😄 empty");
                },

                ok: (output: OutputOk) => {
                    console.log(JSON.stringify(output, null, 4));
                    console.log("😄 ok");
                },

                fail: (err) => {
                    throw err;
                }
            });
        }
    } catch (err) {
        log.print_unknown_error(err);
        console.log("😢 fail.");
    }
}

async function make_input<R>(
    plog: Logger,
    cb: {
        ok: (input: Input, rpc_server: string) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input");

    const args = await yargs(process.argv.slice(2))
        .option("input", {
            type: "string",
            description: "Specify the JSON input directly as a command line argument."
        })
        .option("input-file", {
            type: "string",
            description: "Specify the name of a JSON file to read input from."
        })
        .option("server", {
            type: "string",
            description: "Specify the URL of a remote server to send the request to."
        })
        .option("logger-disabled", {
            type: "boolean",
            description: "Disable logging completely."
        })
        .parse();

    const input = args.input || process.env["x_input"];
    const input_file = args.inputFile || process.env["x_input_file"];
    const server = args.server || process.env["x_server"];
    const logger_disabled = args.loggerDisabled || process.env["x_logger_disabled"] === "true";

    if (logger_disabled) {
        set_logger_disabled(true);
    }

    log.variable("input", input);
    log.variable("input_file", input_file);
    log.variable("server", server);
    log.variable("logger_disabled", logger_disabled);

    if (input && input_file) {
        return cb.fail(log.new_error("Conflicting arguments provided. You can use either --input or --input-file, but not both at the same time."));
    }

    if (input) {
        return make_input_from_args(
            log,
            { json_text: input },
            {
                ok: (input) => {
                    return cb.ok(input, server);
                },
                fail: (err) => {
                    return cb.fail(err);
                }
            }
        );
    } else if (input_file) {
        return make_input_from_file(
            log,
            { filename: input_file },
            {
                ok: (input) => {
                    return cb.ok(input, server);
                },
                fail: (err) => {
                    return cb.fail(err);
                }
            }
        );
    } else {
        return make_input_from_prompts(log, {
            ok: (input) => {
                return cb.ok(input, server);
            },
            fail: (err) => {
                return cb.fail(err);
            }
        });
    }
}

async function make_input_from_args<R>(
    plog: Logger,
    opts: { json_text: string },
    cb: {
        ok: (input: Input) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input_from_args");
    try {
        const input = JSON.parse(opts.json_text);
        log.variable("input", input);

        return check_input(log, input, {
            ok: () => {
                return cb.ok(input);
            },
            fail: (err) => {
                return cb.fail(err);
            }
        });
    } catch (err) {
        log.print_unknown_error(err);
        return cb.fail(err);
    }
}

async function make_input_from_file<R>(
    plog: Logger,
    opts: { filename: string },
    cb: {
        ok: (input: Input) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input_from_file");
    return read_json_file(log, opts.filename, {
        ok: (input) => {
            return check_input(log, input, {
                ok: () => {
                    return cb.ok(input);
                },
                fail: (err) => {
                    return cb.fail(err);
                }
            });
        },
        fail: (err) => {
            return cb.fail(err);
        }
    });
}

async function make_input_from_prompts<R>(
    plog: Logger,
    cb: {
        ok: (v: {
            sort?: {
                field_list: {
                    id?: { order: "ascending" | "descending" };
                    fake?: { order: "ascending" | "descending" };
                    create_time?: { order: "ascending" | "descending" };
                    update_time?: { order: "ascending" | "descending" };
                    finger?: { order: "ascending" | "descending" };
                    token?: { order: "ascending" | "descending" };
                    username?: { order: "ascending" | "descending" };
                    password?: { order: "ascending" | "descending" };
                    avatar_url?: { order: "ascending" | "descending" };
                    real_name?: { order: "ascending" | "descending" };
                    gender?: { order: "ascending" | "descending" };
                    birthday?: { order: "ascending" | "descending" };
                    email?: { order: "ascending" | "descending" };
                    phone_number_country_code?: { order: "ascending" | "descending" };
                    phone_number?: { order: "ascending" | "descending" };
                }[];
            };
            page?: { offset: number; limit: number };
        }) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input_from_prompts");
    try {
        var v: {
            sort?: {
                field_list: {
                    id?: { order: "ascending" | "descending" };
                    fake?: { order: "ascending" | "descending" };
                    create_time?: { order: "ascending" | "descending" };
                    update_time?: { order: "ascending" | "descending" };
                    finger?: { order: "ascending" | "descending" };
                    token?: { order: "ascending" | "descending" };
                    username?: { order: "ascending" | "descending" };
                    password?: { order: "ascending" | "descending" };
                    avatar_url?: { order: "ascending" | "descending" };
                    real_name?: { order: "ascending" | "descending" };
                    gender?: { order: "ascending" | "descending" };
                    birthday?: { order: "ascending" | "descending" };
                    email?: { order: "ascending" | "descending" };
                    phone_number_country_code?: { order: "ascending" | "descending" };
                    phone_number?: { order: "ascending" | "descending" };
                }[];
            };
            page?: { offset: number; limit: number };
        } = {
            sort: await skip_or_input("sort", () =>
                input_sort(log.sub("sort"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                })
            ),
            page: await skip_or_input("page", () =>
                input_page(log.sub("page"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                })
            )
        };
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }

    return cb.ok(v);

    async function input_sort<R>(
        plog: Logger,
        cb: {
            ok: (v: {
                field_list: {
                    id?: { order: "ascending" | "descending" };
                    fake?: { order: "ascending" | "descending" };
                    create_time?: { order: "ascending" | "descending" };
                    update_time?: { order: "ascending" | "descending" };
                    finger?: { order: "ascending" | "descending" };
                    token?: { order: "ascending" | "descending" };
                    username?: { order: "ascending" | "descending" };
                    password?: { order: "ascending" | "descending" };
                    avatar_url?: { order: "ascending" | "descending" };
                    real_name?: { order: "ascending" | "descending" };
                    gender?: { order: "ascending" | "descending" };
                    birthday?: { order: "ascending" | "descending" };
                    email?: { order: "ascending" | "descending" };
                    phone_number_country_code?: { order: "ascending" | "descending" };
                    phone_number?: { order: "ascending" | "descending" };
                }[];
            }) => R;
            fail: (err: Error) => R;
        }
    ): Promise<R> {
        const log = plog.sub("input_sort");
        try {
            var v: {
                field_list: {
                    id?: { order: "ascending" | "descending" };
                    fake?: { order: "ascending" | "descending" };
                    create_time?: { order: "ascending" | "descending" };
                    update_time?: { order: "ascending" | "descending" };
                    finger?: { order: "ascending" | "descending" };
                    token?: { order: "ascending" | "descending" };
                    username?: { order: "ascending" | "descending" };
                    password?: { order: "ascending" | "descending" };
                    avatar_url?: { order: "ascending" | "descending" };
                    real_name?: { order: "ascending" | "descending" };
                    gender?: { order: "ascending" | "descending" };
                    birthday?: { order: "ascending" | "descending" };
                    email?: { order: "ascending" | "descending" };
                    phone_number_country_code?: { order: "ascending" | "descending" };
                    phone_number?: { order: "ascending" | "descending" };
                }[];
            } = {
                field_list: await input_field_list(log.sub("field_list"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                })
            };
        } catch (err) {
            log.error(err);
            return cb.fail(err);
        }

        return cb.ok(v);

        async function input_field_list<R>(
            plog: Logger,
            cb: {
                ok: (
                    v: {
                        id?: { order: "ascending" | "descending" };
                        fake?: { order: "ascending" | "descending" };
                        create_time?: { order: "ascending" | "descending" };
                        update_time?: { order: "ascending" | "descending" };
                        finger?: { order: "ascending" | "descending" };
                        token?: { order: "ascending" | "descending" };
                        username?: { order: "ascending" | "descending" };
                        password?: { order: "ascending" | "descending" };
                        avatar_url?: { order: "ascending" | "descending" };
                        real_name?: { order: "ascending" | "descending" };
                        gender?: { order: "ascending" | "descending" };
                        birthday?: { order: "ascending" | "descending" };
                        email?: { order: "ascending" | "descending" };
                        phone_number_country_code?: { order: "ascending" | "descending" };
                        phone_number?: { order: "ascending" | "descending" };
                    }[]
                ) => R;
                fail: (err: Error) => R;
            }
        ): Promise<R> {
            const log = plog.sub("input_field_list");
            const length = await prompts.input_number("(array.length) field_list");
            const list: {
                id?: { order: "ascending" | "descending" };
                fake?: { order: "ascending" | "descending" };
                create_time?: { order: "ascending" | "descending" };
                update_time?: { order: "ascending" | "descending" };
                finger?: { order: "ascending" | "descending" };
                token?: { order: "ascending" | "descending" };
                username?: { order: "ascending" | "descending" };
                password?: { order: "ascending" | "descending" };
                avatar_url?: { order: "ascending" | "descending" };
                real_name?: { order: "ascending" | "descending" };
                gender?: { order: "ascending" | "descending" };
                birthday?: { order: "ascending" | "descending" };
                email?: { order: "ascending" | "descending" };
                phone_number_country_code?: { order: "ascending" | "descending" };
                phone_number?: { order: "ascending" | "descending" };
            }[] = [];
            for (let i = 0; i < length; ++i) {
                const err = await input_item(log, {
                    ok: (item) => {
                        list.push(item);
                        return null;
                    },
                    fail: (err) => {
                        return err;
                    }
                });

                if (err) {
                    return cb.fail(err);
                }
            }

            return cb.ok(list);

            async function input_item<R>(
                plog: Logger,
                cb: {
                    ok: (v: {
                        id?: { order: "ascending" | "descending" };
                        fake?: { order: "ascending" | "descending" };
                        create_time?: { order: "ascending" | "descending" };
                        update_time?: { order: "ascending" | "descending" };
                        finger?: { order: "ascending" | "descending" };
                        token?: { order: "ascending" | "descending" };
                        username?: { order: "ascending" | "descending" };
                        password?: { order: "ascending" | "descending" };
                        avatar_url?: { order: "ascending" | "descending" };
                        real_name?: { order: "ascending" | "descending" };
                        gender?: { order: "ascending" | "descending" };
                        birthday?: { order: "ascending" | "descending" };
                        email?: { order: "ascending" | "descending" };
                        phone_number_country_code?: { order: "ascending" | "descending" };
                        phone_number?: { order: "ascending" | "descending" };
                    }) => R;
                    fail: (err: Error) => R;
                }
            ): Promise<R> {
                const log = plog.sub("input_item");
                try {
                    var v: {
                        id?: { order: "ascending" | "descending" };
                        fake?: { order: "ascending" | "descending" };
                        create_time?: { order: "ascending" | "descending" };
                        update_time?: { order: "ascending" | "descending" };
                        finger?: { order: "ascending" | "descending" };
                        token?: { order: "ascending" | "descending" };
                        username?: { order: "ascending" | "descending" };
                        password?: { order: "ascending" | "descending" };
                        avatar_url?: { order: "ascending" | "descending" };
                        real_name?: { order: "ascending" | "descending" };
                        gender?: { order: "ascending" | "descending" };
                        birthday?: { order: "ascending" | "descending" };
                        email?: { order: "ascending" | "descending" };
                        phone_number_country_code?: { order: "ascending" | "descending" };
                        phone_number?: { order: "ascending" | "descending" };
                    } = {
                        id: await skip_or_input("id", () =>
                            input_id(log.sub("id"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        fake: await skip_or_input("fake", () =>
                            input_fake(log.sub("fake"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        create_time: await skip_or_input("create_time", () =>
                            input_create_time(log.sub("create_time"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        update_time: await skip_or_input("update_time", () =>
                            input_update_time(log.sub("update_time"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        finger: await skip_or_input("finger", () =>
                            input_finger(log.sub("finger"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        token: await skip_or_input("token", () =>
                            input_token(log.sub("token"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        username: await skip_or_input("username", () =>
                            input_username(log.sub("username"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        password: await skip_or_input("password", () =>
                            input_password(log.sub("password"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        avatar_url: await skip_or_input("avatar_url", () =>
                            input_avatar_url(log.sub("avatar_url"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        real_name: await skip_or_input("real_name", () =>
                            input_real_name(log.sub("real_name"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        gender: await skip_or_input("gender", () =>
                            input_gender(log.sub("gender"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        birthday: await skip_or_input("birthday", () =>
                            input_birthday(log.sub("birthday"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        email: await skip_or_input("email", () =>
                            input_email(log.sub("email"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        phone_number_country_code: await skip_or_input("phone_number_country_code", () =>
                            input_phone_number_country_code(log.sub("phone_number_country_code"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        ),
                        phone_number: await skip_or_input("phone_number", () =>
                            input_phone_number(log.sub("phone_number"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        )
                    };
                } catch (err) {
                    log.error(err);
                    return cb.fail(err);
                }

                return cb.ok(v);

                async function input_id<R>(plog: Logger, cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }): Promise<R> {
                    const log = plog.sub("input_id");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_fake<R>(plog: Logger, cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }): Promise<R> {
                    const log = plog.sub("input_fake");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_create_time<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_create_time");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_update_time<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_update_time");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_finger<R>(plog: Logger, cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }): Promise<R> {
                    const log = plog.sub("input_finger");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_token<R>(plog: Logger, cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }): Promise<R> {
                    const log = plog.sub("input_token");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_username<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_username");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_password<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_password");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_avatar_url<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_avatar_url");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_real_name<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_real_name");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_gender<R>(plog: Logger, cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }): Promise<R> {
                    const log = plog.sub("input_gender");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_birthday<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_birthday");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_email<R>(plog: Logger, cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }): Promise<R> {
                    const log = plog.sub("input_email");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_phone_number_country_code<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_phone_number_country_code");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function input_phone_number<R>(
                    plog: Logger,
                    cb: { ok: (v: { order: "ascending" | "descending" }) => R; fail: (err: Error) => R }
                ): Promise<R> {
                    const log = plog.sub("input_phone_number");
                    try {
                        var v: { order: "ascending" | "descending" } = {
                            order: await input_order(log.sub("order"), {
                                ok: (v) => v,
                                fail: (err) => {
                                    throw err;
                                }
                            })
                        };
                    } catch (err) {
                        log.error(err);
                        return cb.fail(err);
                    }

                    return cb.ok(v);

                    async function input_order<R>(plog: Logger, cb: { ok: (v: "ascending" | "descending") => R; fail: (err: Error) => R }): Promise<R> {
                        const log = plog.sub("input_order");
                        // FIXME implement all string constrains here
                        const v: "ascending" | "descending" = await prompts.input_string_enum("order", ["ascending", "descending"]);
                        return cb.ok(v);
                    }
                }

                async function skip_or_input<R>(field: string, input_cb: () => Promise<R>): Promise<R | undefined> {
                    const v = await prompts.input_string_enum(field, ["skip", "input"]);
                    if (v === "input") {
                        return input_cb();
                    } else {
                        return undefined;
                    }
                }
            }
        }
    }

    async function input_page<R>(plog: Logger, cb: { ok: (v: { offset: number; limit: number }) => R; fail: (err: Error) => R }): Promise<R> {
        const log = plog.sub("input_page");
        try {
            var v: { offset: number; limit: number } = {
                offset: await input_offset(log.sub("offset"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                limit: await input_limit(log.sub("limit"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                })
            };
        } catch (err) {
            log.error(err);
            return cb.fail(err);
        }

        return cb.ok(v);

        async function input_offset<R>(plog: Logger, cb: { ok: (v: number) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_offset");
            // FIXME implement all number constrains here
            const v = await prompts.input_number("offset");
            return cb.ok(v);
        }

        async function input_limit<R>(plog: Logger, cb: { ok: (v: number) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_limit");
            // FIXME implement all number constrains here
            const v = await prompts.input_number("limit");
            return cb.ok(v);
        }
    }

    async function skip_or_input<R>(field: string, input_cb: () => Promise<R>): Promise<R | undefined> {
        const v = await prompts.input_string_enum(field, ["skip", "input"]);
        if (v === "input") {
            return input_cb();
        } else {
            return undefined;
        }
    }
}
