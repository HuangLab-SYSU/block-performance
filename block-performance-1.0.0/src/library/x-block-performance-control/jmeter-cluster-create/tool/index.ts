// auto generated by dev/system

export function make_input(
    cb?: (o: { slave_server_count: number; credential: { aws_lightsail: { access_key_id: string; secret_access_key: string } } }) => void
): { slave_server_count: number; credential: { aws_lightsail: { access_key_id: string; secret_access_key: string } } } {
    const item: { slave_server_count: number; credential: { aws_lightsail: { access_key_id: string; secret_access_key: string } } } = {
        slave_server_count: 0,
        credential: { aws_lightsail: { access_key_id: "", secret_access_key: "" } }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
