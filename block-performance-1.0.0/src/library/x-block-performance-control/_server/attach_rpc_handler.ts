// auto generated by dev/system
import express from "express";
import { Logger } from "../../../myutils/logger.js";
import { handle_rpc_jmeter_cluster_create } from "../jmeter-cluster-create/rpc/handle.js";
import { handle_rpc_jmeter_cluster_remove } from "../jmeter-cluster-remove/rpc/handle.js";
import { handle_rpc_jmeter_cluster_run } from "../jmeter-cluster-run/rpc/handle.js";

export function attach_rpc_handler(plog: Logger, opt: { router: express.Router }) {
    const log = plog.sub("server.attach_rpc_handler");
    const { router } = opt;

    router.post("/library/x-block-performance-control/jmeter-cluster-create", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.x-block-performance-control.jmeter-cluster-create");
        handle_rpc_jmeter_cluster_create(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });

    router.post("/library/x-block-performance-control/jmeter-cluster-remove", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.x-block-performance-control.jmeter-cluster-remove");
        handle_rpc_jmeter_cluster_remove(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });

    router.post("/library/x-block-performance-control/jmeter-cluster-run", (req, res) => {
        const input = req.body;
        const req_log = log.sub("post.library.x-block-performance-control.jmeter-cluster-run");
        handle_rpc_jmeter_cluster_run(req_log, input, {
            invalid_input: (err) => {
                // bad request
                req_log.error(err);
                res.status(400);
                res.end(err.message);
            },
            ok: (result) => {
                // include normal fail case
                req_log.variable("result", result);
                req_log.ok();
                res.json(result);
            },
            fail: (err) => {
                // internal error (not normal fail)
                req_log.error(err);
                res.status(500);
                res.end(err.message);
            }
        });
    });
}
