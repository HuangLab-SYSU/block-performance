// auto generated by dev/system

export function make_input(cb?: (o: { credential: { aws_lightsail: { access_key_id: string; secret_access_key: string } } }) => void): {
    credential: { aws_lightsail: { access_key_id: string; secret_access_key: string } };
} {
    const item: { credential: { aws_lightsail: { access_key_id: string; secret_access_key: string } } } = {
        credential: { aws_lightsail: { access_key_id: "", secret_access_key: "" } }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
