// auto generated by dev/system

export function make_input(cb?: (o: { credentials: { accessKeyId: string; secretAccessKey: string }; region?: string; instanceName: string }) => void): {
    credentials: { accessKeyId: string; secretAccessKey: string };
    region?: string;
    instanceName: string;
} {
    const item: { credentials: { accessKeyId: string; secretAccessKey: string }; region?: string; instanceName: string } = {
        credentials: { accessKeyId: "", secretAccessKey: "" },
        instanceName: ""
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { state: { code: number; name: string } }) => void): { state: { code: number; name: string } } {
    const item: { state: { code: number; name: string } } = { state: { code: 0, name: "" } };
    if (cb) {
        cb(item);
    }
    return item;
}
