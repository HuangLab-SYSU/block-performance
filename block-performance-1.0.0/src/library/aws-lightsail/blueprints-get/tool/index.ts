// auto generated by dev/system

export function make_input(cb?: (o: { credentials: { accessKeyId: string; secretAccessKey: string }; region: string }) => void): {
    credentials: { accessKeyId: string; secretAccessKey: string };
    region: string;
} {
    const item: { credentials: { accessKeyId: string; secretAccessKey: string }; region: string } = {
        credentials: { accessKeyId: "", secretAccessKey: "" },
        region: ""
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(
    cb?: (o: {
        blueprints: {
            blueprintId: string;
            name: string;
            group: string;
            type: string;
            description: string;
            isActive: boolean;
            minPower: number;
            version: string;
            versionCode: string;
            productUrl: string;
            licenseUrl: string;
            platform: string;
        }[];
    }) => void
): {
    blueprints: {
        blueprintId: string;
        name: string;
        group: string;
        type: string;
        description: string;
        isActive: boolean;
        minPower: number;
        version: string;
        versionCode: string;
        productUrl: string;
        licenseUrl: string;
        platform: string;
    }[];
} {
    const item: {
        blueprints: {
            blueprintId: string;
            name: string;
            group: string;
            type: string;
            description: string;
            isActive: boolean;
            minPower: number;
            version: string;
            versionCode: string;
            productUrl: string;
            licenseUrl: string;
            platform: string;
        }[];
    } = { blueprints: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
