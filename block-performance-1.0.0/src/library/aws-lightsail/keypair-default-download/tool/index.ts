// auto generated by dev/system

export function make_input(cb?: (o: { credentials: { accessKeyId: string; secretAccessKey: string }; region?: string }) => void): {
    credentials: { accessKeyId: string; secretAccessKey: string };
    region?: string;
} {
    const item: { credentials: { accessKeyId: string; secretAccessKey: string }; region?: string } = { credentials: { accessKeyId: "", secretAccessKey: "" } };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { createdAt: string; privateKeyBase64: string; publicKeyBase64: string }) => void): {
    createdAt: string;
    privateKeyBase64: string;
    publicKeyBase64: string;
} {
    const item: { createdAt: string; privateKeyBase64: string; publicKeyBase64: string } = { createdAt: "", privateKeyBase64: "", publicKeyBase64: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
