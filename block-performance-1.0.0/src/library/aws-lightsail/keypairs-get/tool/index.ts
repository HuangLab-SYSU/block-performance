// auto generated by dev/system

export function make_input(
    cb?: (o: { credentials: { accessKeyId: string; secretAccessKey: string }; region?: string; includeDefaultKeyPair?: boolean; pageToken?: string }) => void
): { credentials: { accessKeyId: string; secretAccessKey: string }; region?: string; includeDefaultKeyPair?: boolean; pageToken?: string } {
    const item: { credentials: { accessKeyId: string; secretAccessKey: string }; region?: string; includeDefaultKeyPair?: boolean; pageToken?: string } = {
        credentials: { accessKeyId: "", secretAccessKey: "" }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(
    cb?: (o: {
        keyPairs: {
            arn: string;
            createdAt: string;
            fingerprint: string;
            location: { availabilityZone: string; regionName: string };
            name: string;
            resourceType: string;
            supportCode: string;
            tags: { key: string; value: string }[];
        }[];
        nextPageToken: string;
    }) => void
): {
    keyPairs: {
        arn: string;
        createdAt: string;
        fingerprint: string;
        location: { availabilityZone: string; regionName: string };
        name: string;
        resourceType: string;
        supportCode: string;
        tags: { key: string; value: string }[];
    }[];
    nextPageToken: string;
} {
    const item: {
        keyPairs: {
            arn: string;
            createdAt: string;
            fingerprint: string;
            location: { availabilityZone: string; regionName: string };
            name: string;
            resourceType: string;
            supportCode: string;
            tags: { key: string; value: string }[];
        }[];
        nextPageToken: string;
    } = { keyPairs: [], nextPageToken: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
