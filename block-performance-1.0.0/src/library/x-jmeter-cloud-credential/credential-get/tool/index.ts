// auto generated by dev/system

export function make_input(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(
    cb?: (o: {
        credential: {
            id: string;
            fake?: boolean;
            create_time: string;
            update_time: string;
            aws_lightsail: { access_key_id: string; secret_access_key: string };
        };
    }) => void
): {
    credential: { id: string; fake?: boolean; create_time: string; update_time: string; aws_lightsail: { access_key_id: string; secret_access_key: string } };
} {
    const item: {
        credential: {
            id: string;
            fake?: boolean;
            create_time: string;
            update_time: string;
            aws_lightsail: { access_key_id: string; secret_access_key: string };
        };
    } = { credential: { id: "", create_time: "", update_time: "", aws_lightsail: { access_key_id: "", secret_access_key: "" } } };
    if (cb) {
        cb(item);
    }
    return item;
}
