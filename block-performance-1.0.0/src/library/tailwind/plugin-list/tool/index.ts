// auto generated by dev/system

export function make_input(cb?: (o: { css_file: string }) => void): { css_file: string } {
    const item: { css_file: string } = { css_file: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_empty(cb?: (o: { npm_package_list: { name: string }[] }) => void): { npm_package_list: { name: string }[] } {
    const item: { npm_package_list: { name: string }[] } = { npm_package_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { npm_package_list: { name: string }[] }) => void): { npm_package_list: { name: string }[] } {
    const item: { npm_package_list: { name: string }[] } = { npm_package_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
