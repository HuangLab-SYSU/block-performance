// auto generated by dev/system

export function make_input(
    cb?: (o: {
        username: string;
        address: string;
        ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
        from_remote_dir: string;
        to_local_dir: string;
    }) => void
): { username: string; address: string; ssh_auth_key: { os?: boolean; store?: boolean; file?: string }; from_remote_dir: string; to_local_dir: string } {
    const item: {
        username: string;
        address: string;
        ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
        from_remote_dir: string;
        to_local_dir: string;
    } = { username: "", address: "", ssh_auth_key: {}, from_remote_dir: "", to_local_dir: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
