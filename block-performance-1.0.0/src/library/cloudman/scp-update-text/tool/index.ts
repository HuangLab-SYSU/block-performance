// auto generated by dev/system

export function make_input(
    cb?: (o: {
        remote_ssh_auth: { username: string; address: string; ssh_auth_key: { os?: boolean; store?: boolean; file?: string } };
        remote_path: string;
    }) => void
): { remote_ssh_auth: { username: string; address: string; ssh_auth_key: { os?: boolean; store?: boolean; file?: string } }; remote_path: string } {
    const item: {
        remote_ssh_auth: { username: string; address: string; ssh_auth_key: { os?: boolean; store?: boolean; file?: string } };
        remote_path: string;
    } = { remote_ssh_auth: { username: "", address: "", ssh_auth_key: {} }, remote_path: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
