// auto generated by dev/system

export function make_input(
    cb?: (o: {
        username: string;
        address: string;
        ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
        from_local_dir: string;
        to_remote_dir: string;
        pre_installed_applications?: { docker?: {}; python?: {}; build_essential?: {}; chromium_deps?: {} };
    }) => void
): {
    username: string;
    address: string;
    ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
    from_local_dir: string;
    to_remote_dir: string;
    pre_installed_applications?: { docker?: {}; python?: {}; build_essential?: {}; chromium_deps?: {} };
} {
    const item: {
        username: string;
        address: string;
        ssh_auth_key: { os?: boolean; store?: boolean; file?: string };
        from_local_dir: string;
        to_remote_dir: string;
        pre_installed_applications?: { docker?: {}; python?: {}; build_essential?: {}; chromium_deps?: {} };
    } = { username: "", address: "", ssh_auth_key: {}, from_local_dir: "", to_remote_dir: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
