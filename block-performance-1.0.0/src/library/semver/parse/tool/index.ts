// auto generated by dev/system

export function make_input(cb?: (o: { version: string }) => void): { version: string } {
    const item: { version: string } = { version: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { major: number; minor: number; patch: number }) => void): { major: number; minor: number; patch: number } {
    const item: { major: number; minor: number; patch: number } = { major: 0, minor: 0, patch: 0 };
    if (cb) {
        cb(item);
    }
    return item;
}
