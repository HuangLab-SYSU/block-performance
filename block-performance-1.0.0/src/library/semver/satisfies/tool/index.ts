// auto generated by dev/system

export function make_input(cb?: (o: { version: string; range: string }) => void): { version: string; range: string } {
    const item: { version: string; range: string } = { version: "", range: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { satisfied: boolean }) => void): { satisfied: boolean } {
    const item: { satisfied: boolean } = { satisfied: false };
    if (cb) {
        cb(item);
    }
    return item;
}
