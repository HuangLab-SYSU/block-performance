// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    protocol: string;
    username?: string;
    password?: string;
    hostname: string;
    port?: string;
    pathname?: string;
    search_params?: { [key: string]: string };
    hash?: string;
}

export interface OutputOk {
    url: string;
}

export interface OutputFail {}

export interface Callback<R> {
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "protocol") return;
            if (field === "username") return;
            if (field === "password") return;
            if (field === "hostname") return;
            if (field === "port") return;
            if (field === "pathname") return;
            if (field === "search_params") return;
            if (field === "hash") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.protocol must be string");
        if (typeof v.protocol !== "string") {
            throw new Error("v.protocol is not string");
        }

        if (v.username !== undefined) {
            log.println("v.username must be string");
            if (typeof v.username !== "string") {
                throw new Error("v.username is not string");
            }
        }

        if (v.password !== undefined) {
            log.println("v.password must be string");
            if (typeof v.password !== "string") {
                throw new Error("v.password is not string");
            }
        }

        log.println("v.hostname must be string");
        if (typeof v.hostname !== "string") {
            throw new Error("v.hostname is not string");
        }

        if (v.port !== undefined) {
            log.println("v.port must be string");
            if (typeof v.port !== "string") {
                throw new Error("v.port is not string");
            }
        }

        if (v.pathname !== undefined) {
            log.println("v.pathname must be string");
            if (typeof v.pathname !== "string") {
                throw new Error("v.pathname is not string");
            }
        }

        if (v.search_params !== undefined) {
            log.println("v.search_params must be object");
            if (typeof v.search_params !== "object" || v.search_params === null) {
                throw new Error("v.search_params is not object");
            }

            Object.keys(v.search_params).forEach((field) => {
                // a dynamic field, check it (FIXME log message is not clear)

                log.println("v.search_params[field] must be string");
                if (typeof v.search_params[field] !== "string") {
                    throw new Error("v.search_params[field] is not string");
                }
            });
        }

        if (v.hash !== undefined) {
            log.println("v.hash must be string");
            if (typeof v.hash !== "string") {
                throw new Error("v.hash is not string");
            }
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {
            url: copy_url(v.url)
        };
        return obj;
    } else {
        return { url: "" };
    }

    function copy_url(v: any): string {
        return typeof v === "string" ? v : "";
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
