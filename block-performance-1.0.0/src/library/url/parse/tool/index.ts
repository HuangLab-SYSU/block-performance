// auto generated by dev/system

export function make_input(cb?: (o: { url: string }) => void): { url: string } {
    const item: { url: string } = { url: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(
    cb?: (o: {
        protocol: string;
        username: string;
        password: string;
        hostname: string;
        port: string;
        pathname: string;
        search_params: { [key: string]: string };
        hash: string;
    }) => void
): {
    protocol: string;
    username: string;
    password: string;
    hostname: string;
    port: string;
    pathname: string;
    search_params: { [key: string]: string };
    hash: string;
} {
    const item: {
        protocol: string;
        username: string;
        password: string;
        hostname: string;
        port: string;
        pathname: string;
        search_params: { [key: string]: string };
        hash: string;
    } = { protocol: "", username: "", password: "", hostname: "", port: "", pathname: "", search_params: {}, hash: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
