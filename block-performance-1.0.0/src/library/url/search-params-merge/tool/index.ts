// auto generated by dev/system

export function make_input(cb?: (o: { url: string; search_params?: { [key: string]: string } }) => void): {
    url: string;
    search_params?: { [key: string]: string };
} {
    const item: { url: string; search_params?: { [key: string]: string } } = { url: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { url: string }) => void): { url: string } {
    const item: { url: string } = { url: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
