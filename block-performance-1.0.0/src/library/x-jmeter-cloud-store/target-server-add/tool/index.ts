// auto generated by dev/system

export function make_input(cb?: (o: { target_server: { fake?: boolean; cloud_server: { aws_lightsail_instance_id: string } } }) => void): {
    target_server: { fake?: boolean; cloud_server: { aws_lightsail_instance_id: string } };
} {
    const item: { target_server: { fake?: boolean; cloud_server: { aws_lightsail_instance_id: string } } } = {
        target_server: { cloud_server: { aws_lightsail_instance_id: "" } }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { id: string }) => void): { id: string } {
    const item: { id: string } = { id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
