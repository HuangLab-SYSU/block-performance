// auto generated by dev/system

export function make_input(
    cb?: (o: { credential: { id: string; fake?: boolean; aws_lightsail: { access_key_id: string; secret_access_key: string } } }) => void
): { credential: { id: string; fake?: boolean; aws_lightsail: { access_key_id: string; secret_access_key: string } } } {
    const item: { credential: { id: string; fake?: boolean; aws_lightsail: { access_key_id: string; secret_access_key: string } } } = {
        credential: { id: "", aws_lightsail: { access_key_id: "", secret_access_key: "" } }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
