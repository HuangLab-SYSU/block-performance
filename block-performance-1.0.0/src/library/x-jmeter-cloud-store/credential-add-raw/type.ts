// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    credential: { fake?: boolean; create_time: string; update_time: string; aws_lightsail: { access_key_id: string; secret_access_key: string } };
}

export interface OutputOk {
    id: string;
}

export interface OutputFail {}

export interface Callback<R> {
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "credential") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.credential must be object");
        if (typeof v.credential !== "object" || v.credential === null) {
            throw new Error("v.credential is not object");
        }

        Object.keys(v.credential).forEach((field) => {
            if (field === "fake") return;
            if (field === "create_time") return;
            if (field === "update_time") return;
            if (field === "aws_lightsail") return;
            throw new Error("v.credential contains unknown field: " + field);
        });

        if (v.credential.fake !== undefined) {
            log.println("v.credential.fake must be boolean");
            if (typeof v.credential.fake !== "boolean") {
                throw new Error("v.credential.fake is not boolean");
            }
        }

        log.println("v.credential.create_time must be string");
        if (typeof v.credential.create_time !== "string") {
            throw new Error("v.credential.create_time is not string");
        }

        if (v.credential.create_time !== "" && Number.isNaN(Date.parse(v.credential.create_time))) {
            throw new Error("v.credential.create_time is not a valid datetime string");
        }

        log.println("v.credential.update_time must be string");
        if (typeof v.credential.update_time !== "string") {
            throw new Error("v.credential.update_time is not string");
        }

        if (v.credential.update_time !== "" && Number.isNaN(Date.parse(v.credential.update_time))) {
            throw new Error("v.credential.update_time is not a valid datetime string");
        }

        log.println("v.credential.aws_lightsail must be object");
        if (typeof v.credential.aws_lightsail !== "object" || v.credential.aws_lightsail === null) {
            throw new Error("v.credential.aws_lightsail is not object");
        }

        Object.keys(v.credential.aws_lightsail).forEach((field) => {
            if (field === "access_key_id") return;
            if (field === "secret_access_key") return;
            throw new Error("v.credential.aws_lightsail contains unknown field: " + field);
        });

        log.println("v.credential.aws_lightsail.access_key_id must be string");
        if (typeof v.credential.aws_lightsail.access_key_id !== "string") {
            throw new Error("v.credential.aws_lightsail.access_key_id is not string");
        }

        log.println("v.credential.aws_lightsail.secret_access_key must be string");
        if (typeof v.credential.aws_lightsail.secret_access_key !== "string") {
            throw new Error("v.credential.aws_lightsail.secret_access_key is not string");
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {
            id: copy_id(v.id)
        };
        return obj;
    } else {
        return { id: "" };
    }

    function copy_id(v: any): string {
        return typeof v === "string" ? v : "";
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
