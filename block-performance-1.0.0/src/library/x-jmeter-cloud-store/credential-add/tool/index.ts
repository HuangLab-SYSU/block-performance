// auto generated by dev/system

export function make_input(cb?: (o: { credential: { fake?: boolean; aws_lightsail: { access_key_id: string; secret_access_key: string } } }) => void): {
    credential: { fake?: boolean; aws_lightsail: { access_key_id: string; secret_access_key: string } };
} {
    const item: { credential: { fake?: boolean; aws_lightsail: { access_key_id: string; secret_access_key: string } } } = {
        credential: { aws_lightsail: { access_key_id: "", secret_access_key: "" } }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { id: string }) => void): { id: string } {
    const item: { id: string } = { id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
