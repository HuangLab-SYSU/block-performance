// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    target_server: { id: string; fake?: boolean; cloud_server: { aws_lightsail_instance_id: string } };
}

export interface OutputOk {}

export interface OutputFail {}

export interface Callback<R> {
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "target_server") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.target_server must be object");
        if (typeof v.target_server !== "object" || v.target_server === null) {
            throw new Error("v.target_server is not object");
        }

        Object.keys(v.target_server).forEach((field) => {
            if (field === "id") return;
            if (field === "fake") return;
            if (field === "cloud_server") return;
            throw new Error("v.target_server contains unknown field: " + field);
        });

        log.println("v.target_server.id must be string");
        if (typeof v.target_server.id !== "string") {
            throw new Error("v.target_server.id is not string");
        }

        if (v.target_server.fake !== undefined) {
            log.println("v.target_server.fake must be boolean");
            if (typeof v.target_server.fake !== "boolean") {
                throw new Error("v.target_server.fake is not boolean");
            }
        }

        log.println("v.target_server.cloud_server must be object");
        if (typeof v.target_server.cloud_server !== "object" || v.target_server.cloud_server === null) {
            throw new Error("v.target_server.cloud_server is not object");
        }

        Object.keys(v.target_server.cloud_server).forEach((field) => {
            if (field === "aws_lightsail_instance_id") return;
            throw new Error("v.target_server.cloud_server contains unknown field: " + field);
        });

        log.println("v.target_server.cloud_server.aws_lightsail_instance_id must be string");
        if (typeof v.target_server.cloud_server.aws_lightsail_instance_id !== "string") {
            throw new Error("v.target_server.cloud_server.aws_lightsail_instance_id is not string");
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {};
        return obj;
    } else {
        return {};
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
