// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    url: string;
    url_params?: { [key: string]: string };
    headers?: { [key: string]: string };
    proxy_url: string;
}

export interface OutputOk {
    json_value: any;
}

export interface OutputFail {}

export interface Callback<R> {
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "url") return;
            if (field === "url_params") return;
            if (field === "headers") return;
            if (field === "proxy_url") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.url must be string");
        if (typeof v.url !== "string") {
            throw new Error("v.url is not string");
        }

        if (v.url_params !== undefined) {
            log.println("v.url_params must be object");
            if (typeof v.url_params !== "object" || v.url_params === null) {
                throw new Error("v.url_params is not object");
            }

            Object.keys(v.url_params).forEach((field) => {
                // a dynamic field, check it (FIXME log message is not clear)

                log.println("v.url_params[field] must be string");
                if (typeof v.url_params[field] !== "string") {
                    throw new Error("v.url_params[field] is not string");
                }
            });
        }

        if (v.headers !== undefined) {
            log.println("v.headers must be object");
            if (typeof v.headers !== "object" || v.headers === null) {
                throw new Error("v.headers is not object");
            }

            Object.keys(v.headers).forEach((field) => {
                // a dynamic field, check it (FIXME log message is not clear)

                log.println("v.headers[field] must be string");
                if (typeof v.headers[field] !== "string") {
                    throw new Error("v.headers[field] is not string");
                }
            });
        }

        log.println("v.proxy_url must be string");
        if (typeof v.proxy_url !== "string") {
            throw new Error("v.proxy_url is not string");
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {
            json_value: copy_json_value(v.json_value)
        };
        return obj;
    } else {
        return { json_value: null };
    }

    function copy_json_value(v: any): any {
        // the value type here is any
        if (v === undefined || v === null) {
            return v;
        } else if (typeof v === "boolean") {
            return v;
        } else if (typeof v === "number") {
            return v;
        } else if (typeof v === "string") {
            return v;
        } else {
            return JSON.parse(JSON.stringify(v));
        }
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
