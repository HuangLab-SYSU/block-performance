// auto generated by dev/system

export function make_input(cb?: (o: { url: string; headers?: { name: string; value: string }[] }) => void): {
    url: string;
    headers?: { name: string; value: string }[];
} {
    const item: { url: string; headers?: { name: string; value: string }[] } = { url: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { status_code: number; status_message: string; headers: { name: string; value: string }[] }) => void): {
    status_code: number;
    status_message: string;
    headers: { name: string; value: string }[];
} {
    const item: { status_code: number; status_message: string; headers: { name: string; value: string }[] } = {
        status_code: 0,
        status_message: "",
        headers: []
    };
    if (cb) {
        cb(item);
    }
    return item;
}
