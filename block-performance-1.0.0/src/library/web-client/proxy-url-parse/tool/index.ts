// auto generated by dev/system

export function make_input(cb?: (o: { proxy_url: string }) => void): { proxy_url: string } {
    const item: { proxy_url: string } = { proxy_url: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(
    cb?: (o: { proxy: { protocol: string; hostname: string; port: number; auth?: { username: string; password: string } } }) => void
): { proxy: { protocol: string; hostname: string; port: number; auth?: { username: string; password: string } } } {
    const item: { proxy: { protocol: string; hostname: string; port: number; auth?: { username: string; password: string } } } = {
        proxy: { protocol: "", hostname: "", port: 0 }
    };
    if (cb) {
        cb(item);
    }
    return item;
}
