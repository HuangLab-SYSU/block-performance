// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    proxy_url: string;
}

export interface OutputOk {
    proxy: { protocol: string; hostname: string; port: number; auth?: { username: string; password: string } };
}

export interface OutputFail {}

export interface Callback<R> {
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "proxy_url") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.proxy_url must be string");
        if (typeof v.proxy_url !== "string") {
            throw new Error("v.proxy_url is not string");
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {
            proxy: copy_proxy(v.proxy)
        };
        return obj;
    } else {
        return { proxy: { protocol: "", hostname: "", port: 0 } };
    }

    function copy_proxy(v: any): { protocol: string; hostname: string; port: number; auth?: { username: string; password: string } } {
        if (typeof v === "object" && v !== null) {
            const obj = {
                protocol: copy_protocol(v.protocol),
                hostname: copy_hostname(v.hostname),
                port: copy_port(v.port),
                auth: v.auth !== undefined && v.auth !== null ? copy_auth(v.auth) : undefined
            };
            return obj;
        } else {
            return { protocol: "", hostname: "", port: 0 };
        }

        function copy_protocol(v: any): string {
            return typeof v === "string" ? v : "";
        }

        function copy_hostname(v: any): string {
            return typeof v === "string" ? v : "";
        }

        function copy_port(v: any): number {
            return typeof v === "number" ? v : 0;
        }

        function copy_auth(v: any): { username: string; password: string } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    username: copy_username(v.username),
                    password: copy_password(v.password)
                };
                return obj;
            } else {
                return { username: "", password: "" };
            }

            function copy_username(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_password(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
