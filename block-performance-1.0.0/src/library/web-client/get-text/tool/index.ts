// auto generated by dev/system

export function make_input(cb?: (o: { url: string; url_params?: { [key: string]: string }; headers?: { [key: string]: string }; proxy_url: string }) => void): {
    url: string;
    url_params?: { [key: string]: string };
    headers?: { [key: string]: string };
    proxy_url: string;
} {
    const item: { url: string; url_params?: { [key: string]: string }; headers?: { [key: string]: string }; proxy_url: string } = { url: "", proxy_url: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { text: string }) => void): { text: string } {
    const item: { text: string } = { text: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
