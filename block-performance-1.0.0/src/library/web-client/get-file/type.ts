// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    url: string;
    headers?: { name: string; value: string }[];
    save_to_file: string;
}

export interface OutputUnexpectedResponse {
    status_code: number;
    status_message: string;
    headers: { name: string; value: string }[];
}

export interface OutputOk {
    status_code: number;
    status_message: string;
    headers: { name: string; value: string }[];
}

export interface OutputFail {}

export interface Callback<R> {
    unexpected_response: (output: OutputUnexpectedResponse) => R;
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "url") return;
            if (field === "headers") return;
            if (field === "save_to_file") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.url must be string");
        if (typeof v.url !== "string") {
            throw new Error("v.url is not string");
        }

        if (v.headers !== undefined) {
            log.println("v.headers must be array");
            if (!Array.isArray(v.headers)) {
                throw new Error("v.headers is not array");
            }

            v.headers.forEach((item: any, i: number) => {
                log.println("check v.headers[i]");

                log.println("item must be object");
                if (typeof item !== "object" || item === null) {
                    throw new Error("item is not object");
                }

                Object.keys(item).forEach((field) => {
                    if (field === "name") return;
                    if (field === "value") return;
                    throw new Error("item contains unknown field: " + field);
                });

                log.println("item.name must be string");
                if (typeof item.name !== "string") {
                    throw new Error("item.name is not string");
                }

                log.println("item.value must be string");
                if (typeof item.value !== "string") {
                    throw new Error("item.value is not string");
                }
            });
        }

        log.println("v.save_to_file must be string");
        if (typeof v.save_to_file !== "string") {
            throw new Error("v.save_to_file is not string");
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_unexpected_response(v: any): OutputUnexpectedResponse {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_unexpected_response(v_cloned);
    } else {
        return _copy_output_unexpected_response(v);
    }
}

export function _copy_output_unexpected_response(v: any): OutputUnexpectedResponse {
    if (typeof v === "object" && v !== null) {
        const obj = {
            status_code: copy_status_code(v.status_code),
            status_message: copy_status_message(v.status_message),
            headers: copy_headers(v.headers)
        };
        return obj;
    } else {
        return { status_code: 0, status_message: "", headers: [] };
    }

    function copy_status_code(v: any): number {
        return typeof v === "number" ? v : 0;
    }

    function copy_status_message(v: any): string {
        return typeof v === "string" ? v : "";
    }

    function copy_headers(v: any): { name: string; value: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): { name: string; value: string } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    name: copy_name(v.name),
                    value: copy_value(v.value)
                };
                return obj;
            } else {
                return { name: "", value: "" };
            }

            function copy_name(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_value(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {
            status_code: copy_status_code(v.status_code),
            status_message: copy_status_message(v.status_message),
            headers: copy_headers(v.headers)
        };
        return obj;
    } else {
        return { status_code: 0, status_message: "", headers: [] };
    }

    function copy_status_code(v: any): number {
        return typeof v === "number" ? v : 0;
    }

    function copy_status_message(v: any): string {
        return typeof v === "string" ? v : "";
    }

    function copy_headers(v: any): { name: string; value: string }[] {
        return Array.isArray(v) ? v.map(copy_item) : [];

        function copy_item(v: any): { name: string; value: string } {
            if (typeof v === "object" && v !== null) {
                const obj = {
                    name: copy_name(v.name),
                    value: copy_value(v.value)
                };
                return obj;
            } else {
                return { name: "", value: "" };
            }

            function copy_name(v: any): string {
                return typeof v === "string" ? v : "";
            }

            function copy_value(v: any): string {
                return typeof v === "string" ? v : "";
            }
        }
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
