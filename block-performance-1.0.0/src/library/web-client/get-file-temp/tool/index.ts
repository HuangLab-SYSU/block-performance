// auto generated by dev/system

export function make_input(cb?: (o: { url: string; headers?: { name: string; value: string }[]; filename?: string }) => void): {
    url: string;
    headers?: { name: string; value: string }[];
    filename?: string;
} {
    const item: { url: string; headers?: { name: string; value: string }[]; filename?: string } = { url: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_unexpected_response(
    cb?: (o: { status_code: number; status_message: string; headers: { name: string; value: string }[]; temp_file_name: string }) => void
): { status_code: number; status_message: string; headers: { name: string; value: string }[]; temp_file_name: string } {
    const item: { status_code: number; status_message: string; headers: { name: string; value: string }[]; temp_file_name: string } = {
        status_code: 0,
        status_message: "",
        headers: [],
        temp_file_name: ""
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(
    cb?: (o: { status_code: number; status_message: string; headers: { name: string; value: string }[]; temp_file_name: string }) => void
): { status_code: number; status_message: string; headers: { name: string; value: string }[]; temp_file_name: string } {
    const item: { status_code: number; status_message: string; headers: { name: string; value: string }[]; temp_file_name: string } = {
        status_code: 0,
        status_message: "",
        headers: [],
        temp_file_name: ""
    };
    if (cb) {
        cb(item);
    }
    return item;
}
