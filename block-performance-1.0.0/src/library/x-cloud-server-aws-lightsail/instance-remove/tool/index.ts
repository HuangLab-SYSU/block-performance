// auto generated by dev/system

export function make_input(cb?: (o: { id: string; credentials: { accessKeyId: string; secretAccessKey: string } }) => void): {
    id: string;
    credentials: { accessKeyId: string; secretAccessKey: string };
} {
    const item: { id: string; credentials: { accessKeyId: string; secretAccessKey: string } } = {
        id: "",
        credentials: { accessKeyId: "", secretAccessKey: "" }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
