// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";
import { Input, Output, Callback } from "./type.js";
import * as store from "../_/admin/index.js";

export async function core<R>(log: Logger, input: Input, cb: Callback<R>): Promise<R> {
    const admin = await store.get(log, input.id, {
        none: async () => {
            throw log.new_error("not found");
        },
        ok: async (admin) => {
            return admin;
        },
        fail: async (err) => {
            throw err;
        }
    });

    // remove all relations from others

    // remove all relations to others

    // ok, remove the entity

    await store.del(log, admin.id, {
        none: () => {
            throw log.new_error("unexpected not found");
        },
        ok: () => {
            // ignore
        },
        fail: (err) => {
            throw err;
        }
    });

    return cb.ok({});
}
