// auto generated by dev/system

import { Logger, set_logger_disabled } from "../../../myutils/logger.js";
import { prompts } from "../../../myutils/index.js";
import { read_json_file } from "../../../myutils/node/file/index.js";
import { Input, Output, check_input, OutputOk } from "./type.js";
import { admin_add as target_fun } from "./export.js";
import { rpc_admin_add as rpc_target_fun } from "./rpc/client.js";
import yargs from "yargs";

const log = new Logger("cli");
run();

async function run() {
    try {
        const { input, server } = await make_input(log, {
            ok: async (input: Input, server) => {
                return { input, server };
            },
            fail: async (err) => {
                throw err;
            }
        });

        if (server) {
            await rpc_target_fun(
                log,
                {
                    server,
                    input
                },
                {
                    ok: (output) => {
                        console.log(JSON.stringify(output, null, 4));
                        console.log("😄 ok");
                    },

                    fail: (err) => {
                        throw err;
                    }
                }
            );
        } else {
            await target_fun(log, input, {
                ok: (output: OutputOk) => {
                    console.log(JSON.stringify(output, null, 4));
                    console.log("😄 ok");
                },

                fail: (err) => {
                    throw err;
                }
            });
        }
    } catch (err) {
        log.print_unknown_error(err);
        console.log("😢 fail.");
    }
}

async function make_input<R>(
    plog: Logger,
    cb: {
        ok: (input: Input, rpc_server: string) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input");

    const args = await yargs(process.argv.slice(2))
        .option("input", {
            type: "string",
            description: "Specify the JSON input directly as a command line argument."
        })
        .option("input-file", {
            type: "string",
            description: "Specify the name of a JSON file to read input from."
        })
        .option("server", {
            type: "string",
            description: "Specify the URL of a remote server to send the request to."
        })
        .option("logger-disabled", {
            type: "boolean",
            description: "Disable logging completely."
        })
        .parse();

    const input = args.input || process.env["x_input"];
    const input_file = args.inputFile || process.env["x_input_file"];
    const server = args.server || process.env["x_server"];
    const logger_disabled = args.loggerDisabled || process.env["x_logger_disabled"] === "true";

    if (logger_disabled) {
        set_logger_disabled(true);
    }

    log.variable("input", input);
    log.variable("input_file", input_file);
    log.variable("server", server);
    log.variable("logger_disabled", logger_disabled);

    if (input && input_file) {
        return cb.fail(log.new_error("Conflicting arguments provided. You can use either --input or --input-file, but not both at the same time."));
    }

    if (input) {
        return make_input_from_args(
            log,
            { json_text: input },
            {
                ok: (input) => {
                    return cb.ok(input, server);
                },
                fail: (err) => {
                    return cb.fail(err);
                }
            }
        );
    } else if (input_file) {
        return make_input_from_file(
            log,
            { filename: input_file },
            {
                ok: (input) => {
                    return cb.ok(input, server);
                },
                fail: (err) => {
                    return cb.fail(err);
                }
            }
        );
    } else {
        return make_input_from_prompts(log, {
            ok: (input) => {
                return cb.ok(input, server);
            },
            fail: (err) => {
                return cb.fail(err);
            }
        });
    }
}

async function make_input_from_args<R>(
    plog: Logger,
    opts: { json_text: string },
    cb: {
        ok: (input: Input) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input_from_args");
    try {
        const input = JSON.parse(opts.json_text);
        log.variable("input", input);

        return check_input(log, input, {
            ok: () => {
                return cb.ok(input);
            },
            fail: (err) => {
                return cb.fail(err);
            }
        });
    } catch (err) {
        log.print_unknown_error(err);
        return cb.fail(err);
    }
}

async function make_input_from_file<R>(
    plog: Logger,
    opts: { filename: string },
    cb: {
        ok: (input: Input) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input_from_file");
    return read_json_file(log, opts.filename, {
        ok: (input) => {
            return check_input(log, input, {
                ok: () => {
                    return cb.ok(input);
                },
                fail: (err) => {
                    return cb.fail(err);
                }
            });
        },
        fail: (err) => {
            return cb.fail(err);
        }
    });
}

async function make_input_from_prompts<R>(
    plog: Logger,
    cb: {
        ok: (v: {
            admin: {
                fake?: boolean;
                finger: string;
                token: string;
                username: string;
                password: string;
                avatar_url: string;
                real_name: string;
                gender: "unknown" | "male" | "female";
                birthday: string;
                email: string;
                phone_number_country_code: string;
                phone_number: string;
            };
        }) => R;
        fail: (err: Error) => R;
    }
): Promise<R> {
    const log = plog.sub("make_input_from_prompts");
    try {
        var v: {
            admin: {
                fake?: boolean;
                finger: string;
                token: string;
                username: string;
                password: string;
                avatar_url: string;
                real_name: string;
                gender: "unknown" | "male" | "female";
                birthday: string;
                email: string;
                phone_number_country_code: string;
                phone_number: string;
            };
        } = {
            admin: await input_admin(log.sub("admin"), {
                ok: (v) => v,
                fail: (err) => {
                    throw err;
                }
            })
        };
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }

    return cb.ok(v);

    async function input_admin<R>(
        plog: Logger,
        cb: {
            ok: (v: {
                fake?: boolean;
                finger: string;
                token: string;
                username: string;
                password: string;
                avatar_url: string;
                real_name: string;
                gender: "unknown" | "male" | "female";
                birthday: string;
                email: string;
                phone_number_country_code: string;
                phone_number: string;
            }) => R;
            fail: (err: Error) => R;
        }
    ): Promise<R> {
        const log = plog.sub("input_admin");
        try {
            var v: {
                fake?: boolean;
                finger: string;
                token: string;
                username: string;
                password: string;
                avatar_url: string;
                real_name: string;
                gender: "unknown" | "male" | "female";
                birthday: string;
                email: string;
                phone_number_country_code: string;
                phone_number: string;
            } = {
                fake: await skip_or_input("fake", () =>
                    input_fake(log.sub("fake"), {
                        ok: (v) => v,
                        fail: (err) => {
                            throw err;
                        }
                    })
                ),
                finger: await input_finger(log.sub("finger"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                token: await input_token(log.sub("token"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                username: await input_username(log.sub("username"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                password: await input_password(log.sub("password"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                avatar_url: await input_avatar_url(log.sub("avatar_url"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                real_name: await input_real_name(log.sub("real_name"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                gender: await input_gender(log.sub("gender"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                birthday: await input_birthday(log.sub("birthday"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                email: await input_email(log.sub("email"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                phone_number_country_code: await input_phone_number_country_code(log.sub("phone_number_country_code"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                }),
                phone_number: await input_phone_number(log.sub("phone_number"), {
                    ok: (v) => v,
                    fail: (err) => {
                        throw err;
                    }
                })
            };
        } catch (err) {
            log.error(err);
            return cb.fail(err);
        }

        return cb.ok(v);

        async function input_fake<R>(plog: Logger, cb: { ok: (v: boolean) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_fake");
            const v = await prompts.input_boolean("fake");
            return cb.ok(v);
        }

        async function input_finger<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_finger");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("finger", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_token<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_token");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("token", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_username<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_username");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("username", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_password<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_password");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("password", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_avatar_url<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_avatar_url");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("avatar_url", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_real_name<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_real_name");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("real_name", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_gender<R>(plog: Logger, cb: { ok: (v: "unknown" | "male" | "female") => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_gender");
            // FIXME implement all string constrains here
            const v: "unknown" | "male" | "female" = await prompts.input_string_enum("gender", ["unknown", "male", "female"]);
            return cb.ok(v);
        }

        async function input_birthday<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_birthday");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("birthday", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_email<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_email");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("email", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_phone_number_country_code<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_phone_number_country_code");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("phone_number_country_code", { allow_empty: true });
            return cb.ok(v);
        }

        async function input_phone_number<R>(plog: Logger, cb: { ok: (v: string) => R; fail: (err: Error) => R }): Promise<R> {
            const log = plog.sub("input_phone_number");
            // FIXME implement all string constrains here
            const v = await prompts.input_string("phone_number", { allow_empty: true });
            return cb.ok(v);
        }

        async function skip_or_input<R>(field: string, input_cb: () => Promise<R>): Promise<R | undefined> {
            const v = await prompts.input_string_enum(field, ["skip", "input"]);
            if (v === "input") {
                return input_cb();
            } else {
                return undefined;
            }
        }
    }
}
