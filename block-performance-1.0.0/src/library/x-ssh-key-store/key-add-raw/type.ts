// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";

export interface Input {
    key: {
        fake?: boolean;
        create_time: string;
        update_time: string;
        username: string;
        address_list: string[];
        tag_list: string[];
        private_key: string;
        disabled: boolean;
    };
}

export interface OutputOk {
    id: string;
}

export interface OutputFail {}

export interface Callback<R> {
    ok: (output: OutputOk) => R;
    fail: (err: Error) => R;
}

export function check_input<R>(plog: Logger, v: any, cb: { ok: () => R; fail: (err: Error) => R }): R {
    const log = plog.sub("check_input");
    log.variable("v", v);
    try {
        log.println("v must be object");
        if (typeof v !== "object" || v === null) {
            throw new Error("v is not object");
        }

        Object.keys(v).forEach((field) => {
            if (field === "key") return;
            throw new Error("v contains unknown field: " + field);
        });

        log.println("v.key must be object");
        if (typeof v.key !== "object" || v.key === null) {
            throw new Error("v.key is not object");
        }

        Object.keys(v.key).forEach((field) => {
            if (field === "fake") return;
            if (field === "create_time") return;
            if (field === "update_time") return;
            if (field === "username") return;
            if (field === "address_list") return;
            if (field === "tag_list") return;
            if (field === "private_key") return;
            if (field === "disabled") return;
            throw new Error("v.key contains unknown field: " + field);
        });

        if (v.key.fake !== undefined) {
            log.println("v.key.fake must be boolean");
            if (typeof v.key.fake !== "boolean") {
                throw new Error("v.key.fake is not boolean");
            }
        }

        log.println("v.key.create_time must be string");
        if (typeof v.key.create_time !== "string") {
            throw new Error("v.key.create_time is not string");
        }

        if (v.key.create_time !== "" && Number.isNaN(Date.parse(v.key.create_time))) {
            throw new Error("v.key.create_time is not a valid datetime string");
        }

        log.println("v.key.update_time must be string");
        if (typeof v.key.update_time !== "string") {
            throw new Error("v.key.update_time is not string");
        }

        if (v.key.update_time !== "" && Number.isNaN(Date.parse(v.key.update_time))) {
            throw new Error("v.key.update_time is not a valid datetime string");
        }

        log.println("v.key.username must be string");
        if (typeof v.key.username !== "string") {
            throw new Error("v.key.username is not string");
        }

        log.println("v.key.address_list must be array");
        if (!Array.isArray(v.key.address_list)) {
            throw new Error("v.key.address_list is not array");
        }

        v.key.address_list.forEach((item: any, i: number) => {
            log.println("check v.key.address_list[i]");

            log.println("item must be string");
            if (typeof item !== "string") {
                throw new Error("item is not string");
            }
        });

        log.println("v.key.tag_list must be array");
        if (!Array.isArray(v.key.tag_list)) {
            throw new Error("v.key.tag_list is not array");
        }

        v.key.tag_list.forEach((item: any, i: number) => {
            log.println("check v.key.tag_list[i]");

            log.println("item must be string");
            if (typeof item !== "string") {
                throw new Error("item is not string");
            }
        });

        log.println("v.key.private_key must be string");
        if (typeof v.key.private_key !== "string") {
            throw new Error("v.key.private_key is not string");
        }

        log.println("v.key.disabled must be boolean");
        if (typeof v.key.disabled !== "boolean") {
            throw new Error("v.key.disabled is not boolean");
        }
    } catch (err) {
        log.error(err);
        return cb.fail(err);
    }
    // nothing wrong
    log.ok();
    return cb.ok();
}

// JSON stringify value before copy to handle some specific problem
// eg. Date Object probelm
export function copy_output_ok(v: any): OutputOk {
    if (v !== undefined) {
        const v_cloned = JSON.parse(JSON.stringify(v));
        return _copy_output_ok(v_cloned);
    } else {
        return _copy_output_ok(v);
    }
}

export function _copy_output_ok(v: any): OutputOk {
    if (typeof v === "object" && v !== null) {
        const obj = {
            id: copy_id(v.id)
        };
        return obj;
    } else {
        return { id: "" };
    }

    function copy_id(v: any): string {
        return typeof v === "string" ? v : "";
    }
}

// backward compatible purpose
export type Output = OutputOk;
export const copy_output = copy_output_ok;
