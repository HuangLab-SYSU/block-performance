// auto generated by dev/system

export function make_input(
    cb?: (o: {
        key: { id: string; fake?: boolean; username: string; address_list: string[]; tag_list: string[]; private_key: string; disabled: boolean };
    }) => void
): { key: { id: string; fake?: boolean; username: string; address_list: string[]; tag_list: string[]; private_key: string; disabled: boolean } } {
    const item: { key: { id: string; fake?: boolean; username: string; address_list: string[]; tag_list: string[]; private_key: string; disabled: boolean } } =
        { key: { id: "", username: "", address_list: [], tag_list: [], private_key: "", disabled: false } };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
