// auto generated by dev/system

export function make_input(cb?: (o: { name: string; base64: string }) => void): { name: string; base64: string } {
    const item: { name: string; base64: string } = { name: "", base64: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
