// auto generated by dev/system

export function make_input(cb?: (o: { name: string; json_value: any; json_format?: boolean }) => void): {
    name: string;
    json_value: any;
    json_format?: boolean;
} {
    const item: { name: string; json_value: any; json_format?: boolean } = { name: "", json_value: null };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
