// auto generated by dev/system

export function make_input(cb?: (o: { name: string }) => void): { name: string } {
    const item: { name: string } = { name: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { json_value: any }) => void): { json_value: any } {
    const item: { json_value: any } = { json_value: null };
    if (cb) {
        cb(item);
    }
    return item;
}
