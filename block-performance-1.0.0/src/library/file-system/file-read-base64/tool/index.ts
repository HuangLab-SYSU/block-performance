// auto generated by dev/system

export function make_input(cb?: (o: { name: string }) => void): { name: string } {
    const item: { name: string } = { name: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { base64: string }) => void): { base64: string } {
    const item: { base64: string } = { base64: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
